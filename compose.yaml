services:
  device:
    build:
      context: ./Device/.
  
  automation:
    build:
      context: ./Automation/.
    depends_on: 
      - device
    volumes:
      - ./automation_reports:/automation/reports/
